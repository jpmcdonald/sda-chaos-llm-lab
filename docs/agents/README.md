# Agents - Prompt Storage

This directory contains prompts and prompt templates used by agent-based components in the project.

## Structure

- Store prompt templates as `.txt` or `.md` files
- Use descriptive filenames (e.g., `training_policy_prompt.txt`, `regime_analysis_prompt.md`)
- Document prompt versions and use cases

## Usage

[To be filled in]

